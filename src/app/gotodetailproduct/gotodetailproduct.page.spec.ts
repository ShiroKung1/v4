import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GotodetailproductPage } from './gotodetailproduct.page';

describe('GotodetailproductPage', () => {
  let component: GotodetailproductPage;
  let fixture: ComponentFixture<GotodetailproductPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(GotodetailproductPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
